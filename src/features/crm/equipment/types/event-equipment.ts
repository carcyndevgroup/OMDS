export type EventEquipmentAssignment = {
  equipmentCategory: string;
  equipmentId: string;
  equipmentName: string;
  id: string;
  notes: string;
};

export type EventEquipmentFormValues = {
  equipmentId: string;
  notes: string;
};
